# Base URL of the chat backend (portfolio.server), no trailing slash.
# Leave unset to use the live production Render URL hardcoded as a fallback in Chat.tsx.
# For local development against a locally-running portfolio.server:
VITE_API_BASE_URL=http://localhost:3001
